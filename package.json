{
  "dependencies": {
    "yargs": "^13.2.4"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.3",
    "purescript": "^0.13.0",
    "purescript-spago": "^0.6.4"
  },
  "scripts": {
    "dev": "spago build && parcel build --target node index.js && node dist/index.js",
    "build": "spago build && parcel build --target node index.js"
  }
}
